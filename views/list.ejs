<%- include("partials/header.ejs", {listTitle: listTitle}); %>

    <div class="container">
        <div class="heading">
            <h1 class="index-h1"><%- date.toLocaleDateString("en-US", options) %></h1>
        </div>
        <!-- <h1><%= listTitle %></h1> -->
        <div class="content">
            <!-- <div class="item"><input type="checkbox" name="" id=""><p>buy groceries</p></div> 
        <div class="item"><input type="checkbox" name="" id=""><p>refuel the car</p></div>          
        <div class="item"><input type="checkbox" name="" id=""><p>meet Jacob</p></div>
        <div class="item"><input type="checkbox" name="" id=""><p>buy gifts for Max's party</p></div> -->

            <%# for(i=0;i<listItemArray.length;i++){ %>
                <% listItemArray.forEach(element=> { %>
                    <form action="/delete" method="post">
                        <div class="item"><input type="checkbox" name="checkbox" value="<%= element._id %>" onchange="this.form.submit()">
                            <p>
                                <%= element.name %>
                            </p>
                        </div>
                        <input type="hidden" name="listName" value="<%= listTitle %>"></input>
                    </form>
                    <% }); %>
                    <%# } %>
                        <div class="item">
                            <form action="/" method="post">
                                <input type="text" name="newitem" id="newitem" placeholder="add a new item"
                                    autocomplete="off">
                                <button id="formbutton" type="submit" name="list" value="<%= listTitle %>">+</button>
                            </form>
                        </div>
        </div>
    </div>

    <%- include("partials/footer.ejs") %>